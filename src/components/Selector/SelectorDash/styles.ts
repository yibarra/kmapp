import styled from 'styled-components'

export const SelectorDashDiv = styled.div`
  align-items: center;
  display: inline-flex;
  gap: 8px;
  flex-flow: row wrap;
  justify-content: flex-start;
  padding: 0;
  position: relative;
  width: auto;
`

export const SelectorDashContainer = styled.div`
  align-items: flex-start;
  display: flex;
  flex-flow: row wrap;
  gap: 4px;
  position: relative;
  justify-content: flex-start;
`

export const SelectorDashText = styled.p`
  display: flex;
  font-family: "Roboto Mono", monospace;
  font-size: 10px;
  margin: 0;
  position: relative;
  width: 100%;
`
