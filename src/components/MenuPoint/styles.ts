import styled from 'styled-components'

export const MenuPointDiv = styled.div`
  position: absolute;
  transform: translate(0, calc(-100% - 20px));
  transition: all 1s cubic-bezier(0.075, 0.82, 0.165, 1);
`
